{ {{#assign 'randomUUID'}}{{randomValue type='UUID'}}{{/assign}}
    "id": "{{randomUUID}}",
    "foo": "bar",
    "today": "{{now}}",
    "date": "{{now offset='7 days' format='yyyy-MM-dd'}}",
    "filterBy": "{{request.query.filterBy}}",
    "argle": "{{request.query.argle}}",
    "argle1": "{{request.query.argle.0}}",
    "argle2": "{{request.query.argle.1}}",
    "sameAsId": "{{randomUUID}}"
}